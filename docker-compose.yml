version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=file:./dev.db

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
